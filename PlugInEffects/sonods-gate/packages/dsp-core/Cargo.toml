[package]
name = "gate_dsp_core"
version = "0.1.0"
edition = "2021"
description = "SonoDS Gate/Expander DSP core in Rust"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[dev-dependencies]
approx = "0.5"
