ITU-R BS.1770-4 loudness and true-peak measurement.

This crate provides a standards-compliant measurement engine with no
external dependencies (pure `std`), so it can ship to `wasm32-unknown-unknown`
via `wasm-pack` without a JavaScript runtime.
