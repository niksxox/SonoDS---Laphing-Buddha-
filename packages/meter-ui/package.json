{
  "name": "sonods-meter-ui",
  "version": "0.1.0",
  "description": "TypeScript UI layer: Web Components, canvas rendering, interaction",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "dev": "vite",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "format": "prettier --write \"src/**/*.ts\"",
    "test": "vitest"
  },
  "dependencies": {
    "sonods-meter-engine": "workspace:*"
  },
  "peerDependencies": {
    "sonods-meter-engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^3",
    "vite": "^5"
  }
}
